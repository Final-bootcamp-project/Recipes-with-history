export const API_HOME = 'http://localhost:8090';

export const API_URL = (slug) => `${API_HOME}/${slug}`;

export const API_PROFILE = (userId) => `${API_HOME}/profile/${userId}`;

// export const LIKES_URL = (likeId) =>
// 	`https://here-we-will-put-our-heroku-adress.herokuapp.com/${likeId}/like`;
